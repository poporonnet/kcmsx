import { describe, it } from "vitest";

describe("getEntryService", () => {
  it("すべて取得できる", () => {});

  it("チーム名で取得できる", () => {});

  it("チームIDで取得できる", () => {});

  it("存在しないときはエラーを返す", () => {});
});
